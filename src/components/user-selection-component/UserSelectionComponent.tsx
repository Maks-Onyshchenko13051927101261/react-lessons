import {Link} from "react-router";

export const UserSelectionComponent = () => {
    return (
        <nav>
            <Link to="#">Products</Link>
            <Link to="#">Products</Link>
        </nav>
    );
};